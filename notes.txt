// SQL v/s NoSQL

SQL : Stored in RDBMS format (tables, rows, columns) like excel format

SQL : Structured Query Language

Vertically scalable(1 single computer)

// CPU <=> Database Engine
// Code <=> SQL Statements

// Database Engine = what data you want through SQL


Query -> Database Engine


NoSQL : It is not usually RDBMS 
NoSQL : is similar like json {"key": value, "key2" : value} 
NoSQL : Horizontally scalable

1. npm init
2. npm install express --save
3. npm install -g nodemon
   what is nodemon : nodemon is a tool that helps develop node. 
   js based applications by automatically restarting the node application when file changes in the directory are detected.
4. nodemon app => appname 
5. npm install mongodb --save
